# Beads — allowlist form: track ONLY the canonical export + config; every-
# thing else under .beads/ is local scratch (the SQLite db + WAL, the
# .br_history/ auto-snapshots, locks, temp, last-touched). New scratch file
# types are ignored automatically.
*
!.gitignore
!issues.jsonl
!config.yaml
!metadata.json
