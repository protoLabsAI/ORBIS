<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- Tauri's WKWebView still runs JavaScriptCore under the hardened
         runtime even though audio capture is native. Keep the narrow JIT
         exception for the web shell; do not add camera or broader code
         signing exceptions unless a concrete shipped feature requires it. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
</dict>
</plist>
