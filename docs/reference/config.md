# Config (`orbis.yaml`)

The full configuration file. Most options are also reachable from the
**Settings** panel; `orbis.yaml` is the source of truth and the way to
configure ORBIS headlessly.

::: tip Filling out
This reference is being written. For now, the annotated example file is the
best source: [`config/orbis.example.yaml`](https://github.com/protoLabsAI/ORBIS/blob/main/config/orbis.example.yaml).
:::
